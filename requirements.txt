# System tools also needed: poppler (brew install poppler), rmapi (brew install rmapi)

# Required for rendering reMarkable notebooks (.rm files) to PDF
cairosvg>=2.7.0
pdfrw>=0.4
rmc @ git+https://github.com/ricklupton/rmc.git
